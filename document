useEffect(() => {
  const video = videoRef.current;
  if (!video) return;

  Object.defineProperty(video, "playbackRate", {
    get: () => 1.0, // บังคับให้คืนค่าเป็น 1.0
    set: () => {},  // ไม่ทำงานเวลา set
    configurable: false
  });

  video.playbackRate = 1.0; // reset กลับไปที่ 1
}, []);
